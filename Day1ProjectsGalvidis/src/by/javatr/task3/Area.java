package by.javatr.task3;

public class Area {
    public static double sqr(int a, int b) {
        double result = Math.abs(Math.PI * (Math.pow(a, 2) - Math.pow(b, 2)));
        return result;
    }
}
